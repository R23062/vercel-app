name: Supabase Keep Alive

on:
  schedule:
    - cron: '0 0 */4 * *' # 4日ごとに自動実行
  workflow_dispatch: # 手動実行ボタンも作る

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase API
        run: |
          curl -I "${{ secrets.SUPABASE_URL }}"